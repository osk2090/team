<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.osk.team.dao.HotplaceDao">

  <resultMap id="hotplaceMap" type="hotplace">
    <id column="no" property="no"/>
    <result column="title" property="title"/>
    <result column="content" property="content"/>
    <result column="date" property="date"/>
    <result column="count" property="count"/>
    <result column="address" property="address"/>
    <result column="photo" property="photo"/>

    <association property="name" javaType="member">
      <id column="writer_no" property="no"/>
      <result column="writer_name" property="name"/>
    </association>

  </resultMap>

  <insert id="insert" parameterType="hotplace">
    insert into hotplace(title, content, address, photo)
    values (#{title}, #{content}, #{address}, #{photo})
  </insert>

  <update id="update" parameterType="hotplace">
    update hotplace
    set title = #{title},
        content = #{content},
        address = #{address},
        photo = #{photo}
    where  no = #{no}
  </update>

  <delete id="delete" parameterType="int">
    delete from hotplace
    where no = #{value}
  </delete>

</mapper>







